# Word 模板使用说明

## 功能概述

Word 模板解析功能允许你上传 `.docx` 格式的 Word 文档，系统会自动识别文档中的占位符，并将其转换为报告模块。

## 支持的占位符

系统支持以下预定义占位符：

| 占位符 | 说明 | 类型 |
|--------|------|------|
| `{日期}` | 当前日期 | 日期 |
| `{日期范围}` | 报告日期范围 | 日期范围 |
| `{监测边坡}` | 监测边坡名称 | 边坡 |
| `{编制人}` | 报告编制人 | 人员 |
| `{报告标题}` | 报告标题 | 标题 |
| `{报告类型}` | 周报/月报 | 报告类型 |
| `{图表位置}` | 图表插入位置 | 图表 |
| `{表格位置}` | 表格插入位置 | 表格 |
| `{最大值}` | 监测数据最大值 | 数据 |
| `{最小值}` | 监测数据最小值 | 数据 |
| `{平均值}` | 监测数据平均值 | 数据 |

## 示例模板内容

```
{报告标题}

报告类型：{报告类型}
日期范围：{日期范围}
编制人：{编制人}
监测边坡：{监测边坡}

一、工作概述

本周对{监测边坡}进行了全面监测，监测数据正常。

二、监测数据分析

表1：监测数据汇总表
{表格位置}

图1：监测数据趋势图
{图表位置}

数据汇总：
- 最大值：{最大值}mm
- 最小值：{最小值}mm
- 平均值：{平均值}mm

三、结论与建议

根据监测数据分析，边坡稳定性良好，建议继续加强监测。

编制日期：{日期}
```

## 文档结构识别规则

系统会自动识别以下文档结构：

1. **标题** - 以 "第X章"、"一、"、"1." 等开头的段落
2. **表格标题** - 包含 "表" 和 "：" 的段落
3. **图表标题** - 包含 "图" 和 "：" 的段落
4. **正文** - 其他段落内容

## 使用步骤

1. 在 Word 中创建报告模板
2. 在需要动态替换的位置插入占位符（如 `{日期}`）
3. 保存为 `.docx` 格式
4. 在报告生成页面，点击 "上传 Word 模板"
5. 系统会自动解析并生成对应的报告模块
6. 可以映射占位符到系统字段
7. 点击 "应用模板到报告" 完成导入

## 注意事项

1. 仅支持 `.docx` 格式，不支持 `.doc` 格式
2. 文件大小不能超过 10MB
3. 占位符必须使用英文大括号 `{}`
4. 建议使用清晰的文档结构，便于系统正确识别

## 保存为系统模板

解析完成后，你可以：
- 点击 "应用模板到报告" 直接使用
- 点击 "保存为系统模板" 将模板保存到系统，供以后重复使用

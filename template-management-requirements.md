# 模板管理功能需求

## 1. 功能概述

模板管理功能允许用户创建、编辑、保存和使用报告模板，实现报告的标准化和快速生成。

## 2. 核心功能

### 2.1 模板创建
- 用户可以创建新的报告模板
- 模板包含基本信息：模板名称、描述、类型（周报/月报）
- 模板包含可配置的模块列表
- 支持从现有模板复制创建新模板

### 2.2 模板编辑
- 可视化编辑模板结构
- 支持添加、删除、编辑模块
- 支持模块的拖拽排序
- 支持设置模块的默认内容和样式

### 2.3 模板保存和管理
- 保存模板到系统中
- 支持模板的重命名和删除
- 支持模板的分类管理
- 支持模板的搜索和筛选

### 2.4 模板使用
- 从模板列表中选择模板生成报告
- 模板应用后可根据需要修改内容
- 支持模板的版本管理

### 2.5 模板导入导出
- 支持模板的导出为JSON文件
- 支持从JSON文件导入模板
- 支持模板的分享功能

## 3. 技术实现

### 3.1 前端实现
- 使用Vue 3 + Element Plus构建界面
- 使用Sortable.js实现模块拖拽排序
- 使用localStorage或IndexedDB存储模板
- 支持模板的可视化编辑

### 3.2 后端实现
- 提供模板的CRUD API
- 存储模板数据到数据库
- 支持模板的版本控制
- 提供模板的导入导出接口

## 4. 用户界面

### 4.1 模板列表页面
- 展示所有可用模板
- 支持模板的搜索和筛选
- 提供模板的创建、编辑、删除按钮
- 支持模板的预览功能

### 4.2 模板编辑页面
- 可视化的模板编辑界面
- 模块的拖拽排序功能
- 模块的添加和配置界面
- 模板信息的编辑表单

### 4.3 模板使用页面
- 模板选择界面
- 模板应用后的编辑界面
- 报告生成和导出功能

## 5. 数据结构

### 5.1 模板数据结构
```javascript
{
  id: String,           // 模板ID
  name: String,         // 模板名称
  description: String,  // 模板描述
  type: String,         // 模板类型（weekly/monthly）
  modules: Array,       // 模块列表
  createdAt: Date,      // 创建时间
  updatedAt: Date       // 更新时间
}
```

### 5.2 模块数据结构
```javascript
{
  id: String,           // 模块ID
  type: String,         // 模块类型（title/text/chart/table）
  content: String,      // 模块内容
  title: String,        // 模块标题
  level: String,        // 标题级别（h1/h2/h3）
  chartType: String,    // 图表类型（line/bar/pie）
  style: Object         // 模块样式
}
```

## 6. 实现计划

1. 设计模板管理的数据库表结构
2. 实现模板管理的后端API
3. 实现模板列表页面
4. 实现模板编辑页面
5. 实现模板使用功能
6. 实现模板的导入导出功能
7. 集成到现有的报告生成系统

## 7. 预期效果

- 用户可以快速创建和管理报告模板
- 模板可以重复使用，提高报告生成效率
- 报告格式标准化，提高专业性
- 支持个性化定制，满足不同场景需求

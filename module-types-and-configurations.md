# 模块类型和配置选项

## 1. 模块类型概述

报告生成系统支持多种模块类型，每种模块类型具有不同的功能和配置选项，用户可以根据需要选择和配置模块。

## 2. 核心模块类型

### 2.1 标题模块

**功能**：用于添加报告中的标题，支持不同级别的标题。

**配置选项**：
- **标题内容**：标题文本
- **标题级别**：H1、H2、H3
- **字体大小**：可自定义字体大小
- **字体颜色**：可自定义字体颜色
- **对齐方式**：左对齐、居中、右对齐
- **粗体**：是否加粗
- **下划线**：是否添加下划线

**使用场景**：报告的章节标题、小节标题等。

### 2.2 文字模块

**功能**：用于添加报告中的文字内容，支持多行文本。

**配置选项**：
- **文字内容**：文本内容
- **字体大小**：可自定义字体大小
- **字体颜色**：可自定义字体颜色
- **对齐方式**：左对齐、居中、右对齐、两端对齐
- **行间距**：可自定义行间距
- **段落间距**：可自定义段落间距
- **粗体**：是否加粗
- **斜体**：是否斜体
- **下划线**：是否添加下划线
- **项目符号**：是否添加项目符号
- **编号**：是否添加编号

**使用场景**：报告的正文内容、描述性文本等。

### 2.3 图表模块

**功能**：用于添加各种类型的图表，展示数据可视化。

**配置选项**：
- **图表标题**：图表标题
- **图表类型**：折线图、柱状图、饼图、雷达图、散点图
- **数据来源**：手动输入、数据库查询、API接口
- **X轴数据**：X轴坐标数据
- **Y轴数据**：Y轴坐标数据
- **系列名称**：数据系列名称
- **图表尺寸**：宽度和高度
- **图例位置**：顶部、底部、左侧、右侧
- **颜色方案**：预设颜色方案或自定义颜色
- **数据标签**：是否显示数据标签
- **网格线**：是否显示网格线

**使用场景**：数据趋势分析、比较分析、占比分析等。

### 2.4 表格模块

**功能**：用于添加表格，展示结构化数据。

**配置选项**：
- **表格标题**：表格标题
- **表头**：表头内容
- **数据行**：表格数据
- **表格样式**：边框样式、背景颜色
- **列宽**：可自定义列宽
- **行高**：可自定义行高
- **对齐方式**：文本对齐方式
- **边框**：是否显示边框
- **条纹**：是否显示条纹效果
- **数据来源**：手动输入、数据库查询、API接口

**使用场景**：数据汇总、对比分析、详细数据展示等。

### 2.5 图片模块

**功能**：用于添加图片，增强报告的视觉效果。

**配置选项**：
- **图片标题**：图片标题
- **图片来源**：本地上传、URL链接
- **图片尺寸**：宽度和高度
- **对齐方式**：左对齐、居中、右对齐
- **边框**：是否添加边框
- **圆角**：是否添加圆角
- **描述**：图片描述

**使用场景**：现场照片、示意图、流程图等。

### 2.6 引用模块

**功能**：用于添加引用内容，如法规、标准、文献等。

**配置选项**：
- **引用内容**：引用文本
- **引用来源**：引用来源信息
- **引用样式**：块引用、行内引用
- **字体样式**：字体大小、颜色、斜体
- **边框**：是否添加边框
- **背景色**：引用块背景色

**使用场景**：引用法规标准、文献资料、专家观点等。

### 2.7 公式模块

**功能**：用于添加数学公式，支持LaTeX语法。

**配置选项**：
- **公式内容**：LaTeX格式的公式
- **公式编号**：是否添加公式编号
- **对齐方式**：左对齐、居中、右对齐
- **字体大小**：可自定义字体大小

**使用场景**：技术报告中的计算公式、数学模型等。

### 2.8 分隔线模块

**功能**：用于添加分隔线，分隔不同的内容部分。

**配置选项**：
- **分隔线类型**：实线、虚线、点线
- **分隔线颜色**：可自定义颜色
- **分隔线宽度**：可自定义宽度
- **分隔线样式**：居中、左对齐、右对齐

**使用场景**：分隔不同章节、强调重要内容等。

## 3. 模块配置管理

### 3.1 模块属性配置
- 每种模块类型都有对应的属性配置表单
- 配置表单根据模块类型动态生成
- 支持实时预览配置效果

### 3.2 模块样式配置
- 支持全局样式配置和模块级样式配置
- 提供预设样式模板
- 支持自定义样式保存和应用

### 3.3 模块数据配置
- 支持静态数据和动态数据
- 动态数据可从数据库或API获取
- 支持数据过滤和转换

## 4. 模块交互功能

### 4.1 模块拖拽排序
- 支持模块的拖拽排序
- 拖拽时显示视觉反馈
- 排序后自动更新模块顺序

### 4.2 模块复制和粘贴
- 支持模块的复制和粘贴
- 支持跨报告复制模块

### 4.3 模块分组和折叠
- 支持模块的分组管理
- 支持模块的折叠和展开

## 5. 模块数据结构

### 5.1 通用模块结构
```javascript
{
  id: String,           // 模块ID
  type: String,         // 模块类型
  title: String,        // 模块标题
  content: String,      // 模块内容
  config: Object,       // 模块配置
  style: Object,        // 模块样式
  order: Number         // 模块顺序
}
```

### 5.2 配置示例

**标题模块配置**：
```javascript
{
  level: 'h2',
  fontSize: 20,
  color: '#333',
  align: 'center',
  bold: true,
  underline: false
}
```

**图表模块配置**：
```javascript
{
  chartType: 'line',
  dataSource: 'manual',
  xAxis: ['K12+300', 'K12+400'],
  series: [
    { name: '最大值', data: [13.5, 15.2] },
    { name: '最小值', data: [12.5, 14.1] },
    { name: '平均值', data: [13.0, 14.6] }
  ],
  legend: 'top',
  showLabel: true,
  showGrid: true
}
```

## 6. 实现建议

1. **模块化设计**：每种模块类型作为独立组件实现
2. **配置驱动**：通过配置对象控制模块的行为和样式
3. **实时预览**：配置变更时实时更新预览效果
4. **数据绑定**：支持动态数据绑定和更新
5. **扩展性**：设计可扩展的模块系统，支持自定义模块类型

## 7. 预期效果

- 用户可以根据需要选择合适的模块类型
- 每种模块都有丰富的配置选项，满足不同需求
- 模块配置直观易用，支持实时预览
- 模块之间可以灵活组合和排序
- 支持复杂报告的快速构建
